- Signal-handler, um sockets zu schließen bei ctrl+c    -> done
- partial Data receiving... (guck ins subject example)  -> done but not really tested
- PING clients
- checken ob Poll() mit 0, -1 oder was auch immer verwendet wird.. (timeout)
- hostname noch einbauen ?! (aktuell wird die ip als alternative verwendet)

- in Server::receiveMessage loopen, bis alle Daten empfangen wurden
- nick command fertig machen (wenn channels implementiert sind)
- globalen server-pointer ausbauen wenn möglich
- alle libraries auf c++ alternativen überprüfen (z.b. <unistd.h> in main.cpp)